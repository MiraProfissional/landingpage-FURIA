import { render } from "@testing-library/react";
import CardStreamers from ".";

describe("</ CardStreamers>", () => {
	it("should render </CardStreamers>", () => {
		render(<CardStreamers />);
	});
});
